{
  "name": "@repo/agents",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/uvicorn main:app --reload --port 8888",
    "build": "echo 'Python app - no JS build step'",
    "lint": ".venv/bin/ruff check .",
    "start": ".venv/bin/uvicorn main:app --host 0.0.0.0 --port 8888"
  }
}
